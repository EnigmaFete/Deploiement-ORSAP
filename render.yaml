services:
  - type: web
    name: orsap
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: orsap-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
